/*
  @jsbits/escape-regex-str
  @author aMarCruz
  @version 1.0.3 UMD+ES5
  @license MIT
*/
/* eslint-disable */
;(function(root, factory) {
  if (typeof define == 'function' && define.amd) {
    define([], factory)
  } else if (typeof module == 'object' && module.exports) {
    module.exports = factory()
  } else {
    ((root.$ || root.jQuery || {}).jsbits || root.jsbits || (root.jsbits={})).escapeRegexStr = factory()
  }
})(typeof self !== 'undefined' ? self : this, function () {
  "use strict";
  var RE_ESCAPED = /(?=[[\](){}^$.?*+|\\])/g;
  var escapeRegexStr = function _escapeRegexStr(str) {
      return str ? str.replace(RE_ESCAPED, '\\') : '';
  };
  return escapeRegexStr;
});
